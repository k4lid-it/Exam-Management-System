# The_Senior_project file

This is a monorepo, which means that all the files are on one repository. All the pulls and pushs are done here.

In order to start the program write <b><i>"npm run start"</i></b> in the terminal of the root directory <i>(The_senior_project2)</i>. 
